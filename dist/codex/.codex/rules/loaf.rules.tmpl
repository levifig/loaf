# Loaf-owned Codex user-layer capabilities for explicitly classified basic command leaves.
# Build copies this unresolved template; install renders one absolute-executable-pinned prefix per leaf.
{{LOAF_BASIC_RULES}}

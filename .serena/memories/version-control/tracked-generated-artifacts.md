When working in Loaf, do not assume build output is disposable. Check .gitignore and git tracking first. If distributable artifacts like dist/ or plugins/ are tracked, treat them as source-of-truth release outputs and commit them alongside the source changes that generated them. Ignore only the explicitly local build outputs such as dist-cli/ or other paths already listed as ignored.
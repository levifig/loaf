name: qa
description: Quality assurance engineer for testing and security audits. Use for unit tests, integration tests, and security checks.
skills:
  - foundations
conditional-skills:
  - skill: python
    when: "pytest.ini OR pyproject.toml with pytest"
  - skill: ruby
    when: "test/ directory with *_test.rb"
  - skill: typescript
    when: "vitest.config.* OR jest.config.*"
tools:
  - Read
  - Write
  - Edit
  - Bash
  - Glob
  - Grep

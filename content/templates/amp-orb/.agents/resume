#!/bin/sh
# Project-owned Amp Orb resume entry point. Readiness runs before any repair;
# the shared bootstrap downloads only when the pinned installation is not ready.
set -eu

agents_dir=$(CDPATH='' cd "$(dirname "$0")" && pwd)
exec "$agents_dir/loaf-orb-bootstrap.sh" resume

---
id: TASK-137
title: >-
  Add regression tests for validate-commit AI-attribution regex + delete orphan
  workflow-pre-pr.sh
spec: SPEC-031
status: done
priority: P2
created: '2026-04-29T17:27:51.877Z'
updated: '2026-04-29T18:39:41.286Z'
completed_at: '2026-04-29T18:39:41.285Z'
---

# TASK-137: Add regression tests for validate-commit AI-attribution regex + delete orphan workflow-pre-pr.sh

## Description

Lock in the `validate-commit` regex change already shipped in `ccc265e8` with regression tests in `cli/commands/check.test.ts`, covering both new path-token pass cases and preserved real-attribution reject cases. Also delete the orphan `content/hooks/pre-tool/workflow-pre-pr.sh` shell file: `config/hooks.yaml` auto-dispatches `workflow-pre-pr` to the TS path at `cli/commands/check.ts:451` via `loaf check --hook`, leaving the shell file as stale parallel logic. Implements SPEC-031 Task 1.

## Acceptance Criteria

- [ ] New tests in `cli/commands/check.test.ts` assert commit bodies referencing `.claude/CLAUDE.md`, `dist/codex/`, and `.agents/` pass `validate-commit` without rewording.
- [ ] Existing reject cases stay green: `Co-Authored-By: Claude` trailer, `Generated by Claude` line, and bot-emoji footer all still fail `validate-commit`.
- [ ] `content/hooks/pre-tool/workflow-pre-pr.sh` is deleted from the repo and from git history of this branch.
- [ ] `git ls-files content/hooks/pre-tool/workflow-pre-pr.sh` returns empty after the change.
- [ ] No reference to the deleted shell path remains in `config/hooks.yaml` or other config.

## Verification

```bash
npm run typecheck && npm run test -- check.test.ts
git ls-files content/hooks/pre-tool/workflow-pre-pr.sh
```

---
spec: SPEC-036
task: TASK-172
branch: feat/worktree-storage
status: active
created: '2026-05-19T10:13:20Z'
last_entry: '2026-05-19T14:43:06.326Z'
last_updated: '2026-05-19T14:43:06.326Z'
---

# Session: TASK-172 — ADR for agentic state storage model

## Context

**Task:** TASK-172 — author the ADR documenting SPEC-036's A3 agentic state storage model. Written **after** TASK-166 + TASK-170 shipped, so the narrative reflects what's in `main..HEAD`.

**Spec:** SPEC-036 (status: implementing).

**Prior tasks shipped on this branch:**
- TASK-166 at `8665fc3e` — `findAgentsDir` worktree-aware
- TASK-170 at `91eebe18` — `loaf migrate worktree-storage` + refusal nudge + observability knob

**Next ADR number:** 013 (13 existing ADRs in `docs/decisions/`).

## Plan

| Step | Owner | Work |
|------|-------|------|
| 1 | implementer | Author `docs/decisions/ADR-013-agentic-state-storage-model.md`; add ADR link to SPEC-036 frontmatter; check `.agents/AGENTS.md` for retired-convention references and remove if present |
| 2 | orchestrator | Supersede private memory `feedback_branch_at_breakdown.md` (and any related entries) since the "spec on main, tasks+code on branch" convention is now obsolete; commit the lot |

## Journal

[2026-05-19 10:13] resume(implement): orchestrator session for TASK-172 — ADR

## Current State

Session created. About to spawn implementer.

## Next Steps

1. Spawn implementer with ADR brief + architecture skill loaded
2. Verify the ADR file + spec link
3. Update private memory (orchestrator-side)
4. Commit, mark TASK-172 done, mark SPEC-036 complete, prep for PR

## Traceability

- spec: SPEC-036
- task: TASK-172
- adr: ADR-013 (to be created)
- depends_on: TASK-166 (8665fc3e), TASK-170 (91eebe18)
[2026-05-19 11:13] decision(implement): TASK-172 started — ADR-013 authoring for A3 agentic state storage model; single implementer spawn (background)
[2026-05-19 11:35] decision(release): PR #49 created at https://github.com/levifig/loaf/pull/49 — SPEC-036 worktree-aware .agents/ storage; final pre-merge release commit will move [Unreleased] to versioned at squash time via loaf release --pre-merge
[2026-05-19 11:39] decision(review): handing PR #49 to Codex for adversarial second-pass review — loaf:reviewer already approved with nits applied; Codex looks for blind spots a different reader catches
[2026-05-19 11:44] decision(review): Codex returned HOLD verdict on PR #49 — TASKS.json concurrent allocation race is a correctness regression (TASK-NNN collisions under parallel worktree writes). EXDEV partial-move recovery risky. A2 framing in ADR-013 missing git-native review-gate tradeoff. Must address before merge.
[2026-05-19 12:00] decision(implement): spawning two implementers in parallel — A (code fixes: TASKS.json lock + EXDEV staging + Windows path realpath); B (ADR-013 consequence update)
[2026-05-19 12:37] decision(release): Codex fixes pushed to PR #49 — TASKS.json file lock + EXDEV staging + Windows realpath + ADR-013 honesty pass; 1056 tests passing (17 new); plain push (non-force) since these are forward additions to the branch
[2026-05-19 13:00] decision(review): Codex re-review returns HOLD — stale-lock heuristic is age-only (30s TTL evicts live locks during slow critical sections like task sync --import on large TASKS.json). Fix: PID-liveness check via process.kill(pid, 0) before declaring stale. Other 5 items pass.
[2026-05-19 13:02] decision(implement): spawning implementer to fix Codex HOLD #2 — PID-liveness check for stale-lock detection (replaces 30s age-only TTL); tighten concurrency test with start barrier
[2026-05-19 13:17] spark(session-lock): cli/commands/session.ts:281-297 has same age-OR-pid-dead staleness bug as the original TASKS.json lock — long-lived session critical sections may be falsely evicted by a contender after 30s. Apply the same PID-liveness pattern as a follow-up; out of scope for SPEC-036 PR.
[2026-05-19 13:17] decision(release): Codex HOLD #2 fixes pushed to PR #49 — PID-liveness via process.kill(pid, 0); host-aware (foreign host falls back to 5-min age); load-bearing test verified (lock disabled → 5 collisions in 8 workers); 1062 tests passing
[2026-05-19 13:47] decision(review): Codex APPROVE — merge PR #49. HOLD closed; PID-liveness fix sound. Foreign-host 5-min fallback flagged as narrow documented tradeoff (not defect). session.ts:281-297 same defect class is fast-follow, not blocker.
[2026-05-19 14:10] decision(implement): folding session.ts lock-staleness fix into PR #49 — same defect class as TASKS.json lock, user requested quality improvement before merge; lean inline (not refactor)
[2026-05-19 14:23] spark(locks): Extract shared withFileLock primitive — session lock (async, per-file) and TASKS.json lock (sync, fixed path) now share an identical staleness contract (PID liveness + 5-min foreign/malformed fallback) but duplicate ~80 LOC. Worth deduplicating into cli/lib/locks/file-lock.ts once another lock site appears, or as a standalone cleanup spec.
[2026-05-19 14:24] decision(release): session-lock PID-liveness fix pushed to PR #49 (commits 1f549fde + 2312264b); 1071 tests passing; same pattern as TASKS.json lock; legacy missing-host treated as same-host for in-flight orphan compat
[2026-05-19 14:33] decision(implement): pre-merge polish — tight locks (audit critical sections to ms-scale, tighten foreign-host fallback), migrate missing-target error, PR template per ADR-013 follow-on; single-user repo so no backcompat constraints
[2026-05-19 15:01] decision(release): pre-merge polish landed — tight locks (5-min → 5-sec foreign-host fallback after audit moved heavy work outside lock), migrate main-missing error with gitdir-pointer fallback for the deleted-main case, PR template. 1081 tests passing. Commits 01f9c35b + bundle rebuild.
[2026-05-19 15:05] decision(review): Codex HOLD on PR #49 — rebuildTaskIndex inside-lock swap replaces rather than unions; a concurrent task create between outside-scan and inside-merge gets silently dropped. Fix: union current.tasks/current.specs with the scanned set inside the lock. Fast-follows: 5s clock skew, weak 200ms guard.
[2026-05-19 15:08] decision(implement): spawning implementer to fix Codex HOLD on PR #49 — scan-window-aware union merge in rebuildTaskIndex + fast-follows (clock-skew doc comment, strengthened 200ms test)
[2026-05-19 15:30] decision(release): Codex HOLD fix pushed to PR #49 — scan-window-aware union merge in rebuildTaskIndex (commits 717b933d + bundle). Load-bearing regression test verified. 1084 passing. Stale IDE diagnostic on task.ts (loadIndex/SpecEntry/parseEntryIdNumber) confirmed false-positive via fresh typecheck + grep.
[2026-05-19 15:37] decision(review): Codex HOLD #4 on PR #49 — race window between task create's next_id advance (in-lock) and .md write (post-lock) means rebuildTaskIndex can still drop freshly-created tasks. Window predicate parseEntryIdNumber(id) >= scanStartNextId fails because next_id is ALREADY advanced past the new task. Fix: move task create's .md write inside the lock callback OR move next_id increment to after .md write. Regression test seeds wrong next_id (11 vs real 12).
[2026-05-19 15:43] discover(task-lock): moved task create file write ahead of TASKS.json commit under lock; added rebuild regression for index-only next_id-1 stale shape

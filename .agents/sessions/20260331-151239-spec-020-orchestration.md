---
session:
  title: "SPEC-020 Orchestration: Cross-Harness Skills, Hook Consolidation & Target Convergence"
  status: complete
  created: "2026-03-31T15:12:39Z"
  last_updated: "2026-03-31T17:20:00Z"
  branch: "feat/target-convergence"
  spec: "SPEC-020"

traceability:
  requirement: "Cross-harness skill sharing, hook consolidation, and Amp target support"
  architecture:
    - "Shared Content Intermediate"
    - "Hook Backend CLI Commands"
    - "Session Journal Model"
  decisions: []

plans:
  - "20260331-151239-spec-020-orchestration.md"
transcripts: []

orchestration:
  current_task: "TASK-086: Final end-to-end verification - COMPLETE"
  spawned_agents:
    - task: "Wave 1-3"
      status: "completed"
      note: "23 tasks complete"
    - task: "TASK-083"
      status: "completed"
      note: "Amp target"
    - task: "TASK-084"
      status: "completed"
      note: "Codex hooks + install convergence"
    - task: "TASK-085"
      status: "completed"
      note: "Fenced-section management"
    - task: "TASK-086"
      status: "completed"
      note: "End-to-end verification complete"
---

# SPEC-020 Orchestration Session

## Context

Implementing SPEC-020: a foundational architecture overhaul that:
1. **Extracts shared content modules** to eliminate ~1,200 lines of duplicated build logic
2. **Builds a shared skill intermediate** (`dist/skills/`) for all 6 targets
3. **Reworks skill descriptions** for cross-harness routing (two-tier: 250-char truncation + full)
4. **Migrates ~20 non-blocking hooks** to skill instructions (Verification sections)
5. **Creates `loaf check`** CLI backend for 5-6 enforcement hooks
6. **Creates `loaf session`** subcommands for the journal model
7. **Adds Amp target** with runtime plugin
8. **Converges install logic** around `.agents/skills/`

## Current State

- **✓ ALL 26 TASKS COMPLETE**
- **✓ SPEC-020 FULLY IMPLEMENTED**

### Verification Results

**Binary Accessibility:**
- ✓ Claude Code plugin: `./plugins/loaf/bin/loaf --version` → 2.0.0-dev.8
- ✓ Plugin package.json included for version detection
- ✓ Cursor: hooks.json contains `loaf check` commands
- ✓ Codex: hooks.json at `dist/codex/.codex/hooks.json`
- ✓ Amp/OpenCode: Runtime plugins with subprocess calls

**Build & Install:**
- ✓ Clean build: all 6 targets build successfully (amp, codex, cursor, gemini, opencode, skills)
- ✓ Build time: ~0.29s
- ✓ Typecheck: passes
- ✓ Tests: 439/440 pass (1 test infra issue, not code bug)

**SPEC-020 Test Conditions:**
- Phase 1: ✓ Shared intermediate exists, base frontmatter only
- Phase 2: ✓ Skills follow structural convention (Critical Rules → Verification → Quick Reference → Topics)
- Phase 3: ✓ loaf check works for all 5 enforcement hooks, direct commands in hooks.json
- Phase 4: ✓ Amp target builds, Codex hooks output, fenced sections

**Bug Fix Applied:**
- Fixed: Plugin binary version detection (added package.json to plugins/loaf/)

## Post-Implementation Bug Fixes

**Review identified 4 issues — all fixed:**

1. **✓ Fenced section install timing** (cli/commands/install.ts)
   - Fixed: Only install fenced sections for successfully installed targets
   - Before: Fenced sections installed even when tool install was skipped

2. **✓ --detect-linear flag** (cli/commands/session.ts)
   - Fixed: Added `--detect-linear` flag to `loaf session log` command
   - Detects Linear magic words (Fixes/Closes/Resolves + issue IDs) in git commits

3. **✓ conclude entry type** (cli/commands/session.ts)
   - Fixed: Added 'conclude' to VALID_ENTRY_TYPES and EntryType union
   - Now matches documented close-out flow in prompts and hooks

4. **✓ OpenCode relative path** (cli/lib/build/targets/opencode.ts)
   - Fixed: Changed `srcDir: "dist"` to `srcDir: join(rootDir, "dist")`
   - Build now works correctly from any directory, not just repo root

**All fixes verified:** TypeScript passes, 440 tests pass, builds succeed.

SPEC-020 is fully implemented and verified. All 26 tasks across 4 phases are complete.

## Resumption Prompt

Read this session file. **SPEC-020 is COMPLETE**. All 26 tasks implemented and verified. The framework now has:
- Shared content intermediate for all 6 targets
- Consolidated hook backend via `loaf check`
- Session journal model via `loaf session`
- Amp target with runtime plugin
- Direct command generation (no bash wrappers)
- Fenced section management for CLAUDE.md/AGENTS.md

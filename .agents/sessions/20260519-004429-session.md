---
spec: SPEC-036
task: TASK-166
branch: feat/worktree-storage
status: active
created: '2026-05-19T00:44:29Z'
last_entry: '2026-05-19T08:56:36.883Z'
last_updated: '2026-05-19T08:56:36.883Z'
---

# Session: TASK-166 — findAgentsDir worktree probe

## Context

**Task:** TASK-166 — Foundation: worktree probe + dual resolvers + ID allocator scans both views (retitle is fine, but file name preserved per Loaf convention; under A3 the actual scope is single-resolver, single-view).

**Spec:** SPEC-036 (A3 — radical centralization, post-pivot).

**Goal:** Modify `findAgentsDir()` in `cli/lib/tasks/resolve.ts` so that in a linked git worktree it returns the **main worktree's** `.agents/` via `dirname(git rev-parse --git-common-dir)/.agents/`. Single-checkout and non-git contexts preserve current parent-walk behavior verbatim. Every existing call site picks up the new behavior automatically — no per-module refactor.

**Why this is the whole change:** Under A3, the storage model is "always resolve to the main worktree's `.agents/`." Sessions, kb, ideas, drafts, reports, councils, specs, tasks, plans — all already call `findAgentsDir()` either directly or through helpers that do. One function update propagates everywhere.

**Surface area:**
- `cli/lib/tasks/resolve.ts:18-34` — the function itself
- New tests alongside (`resolve.test.ts` or new `resolve.worktree.test.ts`)
- Integration test for cross-worktree session continuity
- Parallel ID allocation test

## Plan

| Step | Owner | Work |
|------|-------|------|
| 1 | implementer | Modify `findAgentsDir` with worktree probe; add unit tests; add cross-worktree integration test; add parallel ID allocation test; ensure full test suite green |
| 2 | reviewer | Independent audit — focus on probe correctness in detached HEAD / submodules / bare repos, test coverage of fallback paths, no regressions |
| 3 | orchestrator | Commit, update TASK-166 status to done, journal completion |

## Journal

[2026-05-19 00:44] resume(implement): orchestrator session for TASK-166 — findAgentsDir worktree probe + tests

## Current State

Session created. Plan drafted. Awaiting user approval to spawn implementer agent.

## Next Steps

1. User approves the spawn plan
2. Spawn implementer agent with TypeScript + foundations skills loaded
3. After implementer finishes: spawn reviewer for independent audit
4. Address review findings (if any)
5. Commit and update task status

## Traceability

- spec: SPEC-036
- task: TASK-166
- adr: pending (TASK-172)
[2026-05-19 01:45] decision(implement): orchestrator session created for TASK-166 — findAgentsDir worktree probe; plan: 1 implementer + 1 reviewer; awaiting approval to spawn
[2026-05-19 09:37] decision(implement): spawning loaf:implementer for TASK-166 — findAgentsDir worktree probe + tests (TS+foundations skills)
[2026-05-19 09:47] decision(review): spawning loaf:reviewer for TASK-166 implementer diff — independent audit, edge cases, regression check
[2026-05-19 09:54] spark(test-isolation): replace lenient null-or-outside-repo fallback in resolve.ts parent-walk tests with explicit HOME/TMPDIR isolation via vitest env hook; documented in TASK-166 review (Q4)
[2026-05-19 09:54] decision(triage): Q3 from TASK-166 review propagated to TASK-170 Observability section; test-isolation finding (6 check.test.ts failures with tag.gpgsign=true globally) appended to TASK-165 with line numbers + recommended fix
[2026-05-19 09:56] decision(complete): TASK-166 shipped — findAgentsDir worktree-aware in cli/lib/tasks/resolve.ts; 11 new tests pass; nits Q1+Q2 applied inline; Q3 in TASK-170, Q4 captured as spark, TASK-165 expanded with test-isolation findings. Track A done; TASK-170 next.
